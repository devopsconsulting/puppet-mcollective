topicprefix = /topic/
main_collective = mcollective
collectives = mcollective,<%= collectives.join(',') %>
libdir = <% if @operatingsystem == "Fedora" -%>/usr/libexec/mcollective<% else -%>/usr/share/mcollective/plugins<% end %>
logger_type = console
loglevel = warn

# Plugins
securityprovider = psk
plugin.psk = tR=a!2iafr0a

connector = stomp
plugin.stomp.host = <%= stomp_host.nil? ? "localhost" : stomp_host %>
plugin.stomp.port = 6163
plugin.stomp.user = mcollective
plugin.stomp.password = _9_ehoE8ria1

# Facts
factsource = yaml
plugin.yaml = /etc/mcollective/facts.yaml

